<div id="cadastro">
	<h1>  Perfil </h1>
    <fieldset class="pequeno">
    	<legend>Cadastro de Perfis</legend>	
		<%= form_for(@perfil) do |f| %>
		  <% if @perfil.errors.any? %>
		    <div id="error_explanation">
		      <h2><%= pluralize(@perfil.errors.count, "error") %> prohibited this perfil from being saved:</h2>
		      <ul>
		      <% @perfil.errors.full_messages.each do |msg| %>
		        <li><%= msg %></li>
		      <% end %>
		      </ul>
		    </div>
		  <% end %>
		  	<div class="actions">
				<%= f.label :nome %>
				<%= f.text_field :nome %>
				<%= f.label 'Permissão'%>
				<%= f.collection_select :permissao_id, Permissao.find(:all, :order => 'nome'), :id, :nome, {}, :multiple => true %>
				<%= f.submit %>
				<%= link_to 'Voltar', perfils_path %>
		  	</div>
		<% end %>
	</fieldset>
</div>
