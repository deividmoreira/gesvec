<h1>Listing motorista</h1>

<table>
  <tr>
    <th>Nome</th>
    <th>Cpf</th>
    <th>Cnh</th>
    <th>Status</th>
    <th>Data validade</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @motorista.each do |motoristum| %>
  <tr>
    <td><%= motoristum.nome %></td>
    <td><%= motoristum.cpf %></td>
    <td><%= motoristum.cnh %></td>
    <td><%= motoristum.status %></td>
    <td><%= motoristum.data_validade %></td>
    <td><%= link_to 'Show', motoristum %></td>
    <td><%= link_to 'Edit', edit_motoristum_path(motoristum) %></td>
    <td><%= link_to 'Destroy', motoristum, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Motoristum', new_motoristum_path %>
